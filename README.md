# DD_AI_project_2026

запуск проекта `docker compose up --build` -- перый раз

запуск проекта `docker compose up ` -- потом чтоб дублей небыло

для фронта `docker-compose restart frontend` -- если код поменял

остановка `docker compose down`